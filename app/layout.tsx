import type { Metadata } from "next";
import { Inter, Playfair_Display, Geist_Mono } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

const playfair = Playfair_Display({
  variable: "--font-playfair",
  subsets: ["latin"],
  display: "swap",
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "ALVI’S | Wear the Legacy",
    template: "%s | ALVI’S",
  },
  description:
    "ALVI’S is a luxury menswear label focused on restrained tailoring, bespoke craftsmanship, and timeless formalwear.",
  applicationName: "ALVI’S",
  keywords: ["menswear", "tailoring", "bespoke suits", "formalwear", "luxury fashion"],
  creator: "ALVI’S",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${inter.variable} ${playfair.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
